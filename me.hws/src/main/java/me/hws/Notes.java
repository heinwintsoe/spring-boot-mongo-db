package me.hws;

/**
 * To Note Down about the project
 * 
 * @author Hein Wint Soe
 */
public class Notes {

	/* 
	 
	 Run the following query for MongoDB auto sequence generation:	 
	   db.sequence.insert({_id: "<SequenceKeyName>", sequence: 0})
	   Eg:
	     db.sequence.insert({_id: "ProjectSequence", sequence: 0})
	  
	 */
	
}
